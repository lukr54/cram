# Third-party licence texts

Full licence text for the third-party crates statically linked into Cram's binaries, for the shipped
target `x86_64-pc-windows-gnu` with the release feature set `download,zstd-c`. Generated by
`cargo about` from each crate's own `LICENSE` file. This file is distributed alongside the executables;
see `THIRD-PARTY-NOTICES.md` for the hand-curated notices (UnRAR, the bundled C zstd library, etc.).

Summary by licence:

{{#each overview}}
- **{{{name}}}** — {{count}} crate(s)
{{/each}}
{{#each licenses}}

---

## {{{name}}}

Applies to:
{{#each used_by}}
- `{{crate.name}}` {{crate.version}}
{{/each}}

```text
{{{text}}}
```
{{/each}}
